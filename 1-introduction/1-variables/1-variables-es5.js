//on assigne la valeur du type primitif 4 dans primitiveType
var primitiveType = 4;

//on assigne l'adresse du type reference {} dans referenceType
var referenceType = {};

//on utilise le module console qui est accessible sur le context global
console.log(primitiveType);

//on peut reassigner la variable, avec un type différent
i = 'les amis';
console.log(primitiveType);
